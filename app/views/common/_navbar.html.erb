<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">

    <div class="navbar-header">
      <a class="navbar-brand" href="/">ChuLab</a>
    </div>

    <div class="">
      <ul class="nav navbar-nav">
        <li> <%= link_to("Meeting時程表", meetings_path) %> </li>
        <li> <%= link_to("藥品清單", chemicals_path) %></li>
        <li> <%= link_to("光學元件", optical_elements_path) %></li>
        <li> <%= link_to("筆記板", posts_path) %></li>
        <li> <%= link_to("成員資料", users_path) %></li>
      </ul>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if !current_user %>
          <li><%= link_to("註冊", new_user_registration_path)%></li>
          <li><%= link_to("登入", new_user_session_path) %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              安安！
              <% if current_user.profile != nil && current_user.profile.name != nil %>
                <%= current_user.profile.name %> ～
              <% else %>
                <%= current_user.email %> ～
              <% end %>
              實驗還順利嗎？
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to("登出", destroy_user_session_path, method: :delete) %></li>
              <li><%= link_to("編輯個人資料", edit_account_user_path(current_user)) %></li>
              <li><%= link_to("我的筆記", account_posts_path) %></li>
              <li><%= link_to("我的收藏", account_collects_path) %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>

  </div>
</nav>
