<%= simple_form_for @category do |f| %>

  <div class="form-group">
    <%= f.input :name, label: "類別名稱", :autofocus => true, input_html:{class: "form-control"} %>
  </div>
    <b>星號 * 表示必填</b>
    <br></br>
  <%= f.submit "送出", class:"btn btn-primary", data: {disable_with: "送出中..."} %>

<% end %>
